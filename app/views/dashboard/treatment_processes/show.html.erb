Treatment page for <%= @treatment.questionaire.title %>
<%= @message %>

<% if @treatment.evaluations.any? %>
  <% @treatment.evaluations.each do |evaluation| %>
    <%= link_to "#{evaluation.created_at}", dashboard_treatment_evaluation_path(@treatment, evaluation)  %>
  <% end %>
<% end %>